Sprint 4.5 Housekeeping Step 4A - Validation

Step 4A is structural cleanup only; no runtime Python code is changed.

From the repository root run:

  py -m unittest discover -s tests -v

Expected baseline:

  Ran 123 tests
  OK

Optional deterministic sanity checks (not required if the full suite passes):

  py -m tools.bom_normalization_check "input\gb-mini-board-v0.1-draft2.csv"
  py -m tools.parts_master_seed_import_check "input\AIPN Parts Master.xlsx"

Do not use existing output files as evidence. If running the optional checks, regenerate them from input/.
