Sprint 4.6.2 test plan

1. Full regression suite:
   py -m unittest discover -s tests -v

2. Fresh local BOM match from source inputs (no prior output is used):
   py -m tools.bom_existing_knowledge_match_check "input\gb-mini-board-v0.1-draft2.csv" --parts-master "input\AIPN Parts Master.xlsx"

3. Review:
   output\bom_review\gb-mini-board-v0.1-draft2__LOCAL_MATCH.csv
   output\bom_review\gb-mini-board-v0.1-draft2__LOCAL_MATCH_SUMMARY.json

Acceptance checks:
- 163 source rows -> 75 normalised rows.
- DNP rows, if present, are processed rather than excluded.
- Value+Footprint matches are candidates only.
- Provider calls = 0.
- Automatic approvals = 0.
- Existing output files are not used as source data.
