Sprint 4.5 Housekeeping Step 3 - validation plan

1. Full regression suite

   py -m unittest discover -s tests -v

   Baseline expectation: 123 tests, OK.

2. Fresh BOM normalisation from committed input

   py -m tools.bom_normalization_check "input\gb-mini-board-v0.1-draft2.csv"

   Baseline:
     Source rows       163
     Normalised rows    75
     Consolidated       88
     Traceability     PASS

3. Fresh Parts Master staging import from committed input

   py -m tools.parts_master_seed_import_check "input\AIPN Parts Master.xlsx"

   Baseline:
     Source rows                 263
     Staging records             260
     Incomplete identity rows      0
     Duplicate identity groups     3
     Manufacturer alias groups     4
     Automatic approvals           0
     New AIPNs allocated           0
     Traceability               PASS

4. 10-part Knowledge Base population smoke test using the FRESH staging file

   py -m tools.knowledge_base_population_check "output\parts_master_staging\AIPN_Parts_Master__STAGING.csv" --limit 10

   Judge provider-backed tests by successful workflow/connectivity/parsing, not exact live commercial values.

5. Provider-specific checks remain unchanged and are run from their provider packages:

   py -m providers.digikey.checks.profile_check
   py -m providers.mouser.checks.connectivity_check MCP1711T-25I/OT
   py -m providers.mouser.checks.profile_check
   py -m providers.tme.checks.connectivity_check MCP1711T-25I/OT
   py -m providers.tme.checks.profile_check

6. Generic provider onboarding helpers smoke test

   py -m tools.provider_onboarding.inspect_json --help
   py -m tools.provider_onboarding.attribute_gap_analysis --help

Do not use prior output files as test inputs where a fresh upstream output can be regenerated.
