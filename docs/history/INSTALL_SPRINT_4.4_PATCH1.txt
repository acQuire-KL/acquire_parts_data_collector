PDC v0.2.10 - Sprint 4.4 Patch 1
PDC Parts Master Seed Import

1. Copy the patch contents into the project root, preserving folders.
2. Run the regression suite:

   py -m unittest discover -s tests -v

3. Run the legacy Parts Master import:

   py parts_master_seed_import_check.py "C:\path\to\AIPN Parts Master.xlsx"

4. Review files under:

   output\parts_master_staging\

This patch does not query providers, allocate AIPNs, approve records, or modify
the source workbook.
