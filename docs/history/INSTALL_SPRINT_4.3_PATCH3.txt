PDC v0.2.10 - Sprint 4.3 Patch 3
Natural Reference Sorting

INSTALLATION
1. Extract the contents of this patch over the project root.
2. Preserve the folder structure.
3. Run:

   py -m unittest discover -s tests -v

4. Re-run the BOM normaliser:

   py bom_normalization_check.py "C:\path\to\gb-mini-board-v0.1-draft2.csv"

EXPECTED BEHAVIOUR
- __NORMALISED.csv is sorted by the first Reference Designator using natural order.
- __NORMALISED_DEBUG.csv uses the same order.
- Combined references remain naturally sorted within each row.
- SOURCE.csv, TRACE.json and SUMMARY.json are unchanged in purpose and structure.
- Grouping, DNP separation, quantity totals and traceability are unchanged.
