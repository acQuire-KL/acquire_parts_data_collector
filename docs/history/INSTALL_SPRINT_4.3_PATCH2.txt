PDC v0.2.10 - Sprint 4.3 Patch 2
Clean Normalised Output + Separate Debug Output

INSTALLATION
1. Extract the contents of this patch over the project root.
2. Preserve the supplied folder structure.
3. No files need to be deleted.

TEST
  py -m unittest discover -s tests -v

RUN
  py bom_normalization_check.py "C:\path\to\your_bom.csv"

OUTPUTS
  <name>__SOURCE.csv
  <name>__NORMALISED.csv
  <name>__NORMALISED_DEBUG.csv
  <name>__TRACE.json
  <name>__SUMMARY.json

The clean NORMALISED.csv excludes:
- Grouping Basis
- Source Rows
- Source Data JSON

Those fields remain in NORMALISED_DEBUG.csv, while TRACE.json remains the
formal lossless traceability record.
